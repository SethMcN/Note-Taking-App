// TODO(stores): Create workspace store exposing root path, open folders, and metadata.
// TODO(stores): Maintain recent workspaces list and current workspace reference.
// TODO(stores): Sync workspace state with persistence layer on changes.