// TODO(stores): Create settings store with font, editor, and explorer preferences.
// TODO(stores): Persist user settings to local storage or backend.
// TODO(stores): Merge workspace-level overrides with global settings.