// TODO(stores): Expose theme selection (light/dark/auto) as writable store.
// TODO(stores): Derive computed system theme detection and effective theme.
// TODO(stores): Apply CSS variable updates when theme changes.