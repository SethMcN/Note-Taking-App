// TODO(filesystem): Expose readFolder, readFile, writeFile, deleteFile, and stat operations with error handling.
// TODO(filesystem): Cache file metadata and invalidate on watcher events.
// TODO(filesystem): Provide typed result wrappers instead of throwing raw backend errors.