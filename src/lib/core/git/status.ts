// TODO(git): Query git status and return file decorations (modified, staged, untracked, conflicted).
// TODO(git): Cache status results and update on filesystem watcher events.
// TODO(git): Expose decoration symbols for explorer tree rendering (M, A, D, ?, !).