// TODO(hooks): Create hotkey registration store with global keyboard listener.
// TODO(hooks): Support context-specific hotkey bindings (editor vs explorer mode).
// TODO(hooks): Allow user customization of keybindings with conflict detection.